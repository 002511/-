# Task Status

更新时间：2026-06-18 15:55:00 +08:00

用途：记录当前任务、验证状态、遗留问题和下一步建议。当前事实以 `docs/CURRENT_FACTS.md` 为准。

## 当前任务

| 任务 | 状态 | 说明 |
|---|---|---|
| 文档治理与低上下文接手优化 | 已完成 | 创建 `AGENTS.md` 与 `docs/` 文档体系，降权旧文档误导内容 |
| 开发经验与前端修改经验沉淀 | 已完成 | 新增 `docs/DEVELOPMENT_NOTES.md`，整理路径坑、前端页面定位、五阶段工作台、新建项目弹窗、Playwright、打包安装和文档同步规则 |
| RewritePage 响应式布局修复 | 已完成 | 历史修复曾采用中屏两栏；当前原应用式 UI 已改为 1366x768 / 1400x900 / 1920x1080 均保持三栏且无横向滚动 |
| WebView2 窗口参数对齐 | 已完成 | 默认 `1400x900`，最小 `1280x800`，`ZoomFactor=1.0` |
| 最新安装包安装后桌面行为验收 | 已完成 | 重新运行 Setup，确认安装目录、快捷方式、WebView2 子进程、随机本地端口和原库时间戳 |
| 原 FleshOut DB 路径配置化 | 已完成 | 新增 `GET/PUT /api/settings` 和设置页表单，保存到安装根目录 `config.json` |
| 旧源码路径自建项目删除失败修复 | 已完成 | 删除安全判断兼容源码/安装版 `data/user-projects`，原库项目不再显示删除按钮 |
| 原 FleshOut 页面风格 UI 设计 | 已完成 | 项目入口、五阶段暗色工作台、开始/继续阶段操作条、单章改写操作区已按截图流程重做 |
| 项目入口分段筛选模块移除 | 已完成 | 已去掉截图红框中的 `全部 / 最近更新 / 原 FleshOut / 自建项目` 分段筛选，保留搜索和项目列表 |
| 项目详情运行目录隐藏 | 已完成 | 工作台项目详情页不再展示 `运行目录` 文件夹统计块，阶段页中需要检查输出目录的统计块仍保留 |
| 原 FleshOut 项目工作流操作权限开放 | 已完成 | 原项目可在本应用内同步 sidecar 状态并执行五阶段工作流；新增状态写 sidecar / `.fleshout-workspace`，原 `fleshout.db` 仍只读 |
| 五阶段工作台顺序门禁 | 已完成 | 新上传项目必须先完成 `书籍拆分` 才能进入 `内容总结`；后续步骤需前置步骤全部完成后才可查看和操作，后端接口同步拦截绕过 UI 的调用 |
| 书籍拆分页对齐原应用 | 已完成 | 第 1 步中间只保留书籍名称，隐藏书籍基本信息和文件信息；右侧只保留书籍拆分进度条和开始/继续按钮，不显示改写相关动作 |
| 内容总结页对齐原应用 | 已完成 | 第 2 步未完成时保持原应用式开始/继续流程；完成后留在内容总结页，左侧章节列表显示 `需改写` / `无需改写` / `已总结` 标识，点击章节显示情节概要、登场人物、关键事件；需改写章节下方显示提示词模板匹配出的场景识别条件和改写规则 |
| 新建项目导入向导左侧颜色修正 | 已完成 | 左侧当前步骤改为暗色选中态和绿色圆点高亮，避免浅色块与弹窗左上角黑灰底色不一致 |
| 新建项目上传第一步对齐原应用 | 已完成 | 上传 TXT 后显示文件名卡片和项目名称确认，不再显示 TXT 正文；右下角 `下一步` 进入 `TXT 拆分` |
| TXT 拆分预览流程对齐原应用 | 已完成 | 点击 `预览` 后停留在 `TXT 拆分` 页，章节列表显示行号，底部保留 `下一步` |
| 预览信息总字数格式对齐原应用 | 已完成 | 第 3 步 `预览信息` 的总字数显示为 1 位小数 `万字`，不再显示纯数字加 `字` |
| 模型配置页处理参数对齐原应用 | 已完成 | 第 4 步只显示自动模式，并提供并发处理数和加料字数设置；创建项目时保存为 `auto` 模式，阶段 2/3/4 使用项目并发数执行 |
| 提示词策略页对齐原应用 | 已完成 | 第 5 步支持切换并编辑系统破甲、场景识别和改写规则；场景识别可展开/收起，改写规则可编辑通用和场景规则；编辑后创建项目会克隆 sidecar 项目专属模板 |
| 工作台编辑项目配置弹窗 | 已完成 | 项目详情点击 `编辑` 现在打开原应用式 `项目配置`，包含基础设置、AI 模型、提示词策略；自建项目可保存模型、模板、并发和加料字数，原 FleshOut 项目仍只保存显示名 |
| 新建项目弹窗滚动穿透修复 | 已完成 | 弹窗打开时锁定 body 滚动，鼠标滚轮不再带动上级工作台页面 |
| 业务代码重构 | 未执行 | 本次任务明确要求不要大规模重构业务代码 |
| 真实 AI 调用 | 未执行 | 本次仅验证上传向导 UI、TXT 拆分预览和滚动行为，不触发外部模型调用 |

## 当前已记录验证

| 验证项 | 状态 | 本次是否重跑 | 依据 |
|---|---|---|---|
| `node --check server/index.js` | 通过 | 是 | 2026-06-18 内容总结页对齐后执行通过 |
| `npm run build` | 通过 | 是 | 2026-06-18 内容总结页对齐后执行通过，Vite 构建成功 |
| `npx tsc --noEmit` | 未通过 | 是 | 项目未安装 `@types/react` / `@types/react-dom`，现有类型声明缺失导致大量 JSX 类型错误；实际构建使用 `vite build` 已通过 |
| `npm run dist:win` | 通过 | 是 | 2026-06-18 书籍拆分页 UI 收敛后执行通过，重新生成安装包 |
| Windows 安装包生成 | 通过 | 是 | `release/FleshOut-Compatible-Setup.exe` 已重新生成，`LastWriteTimeUtc=2026-06-18T07:30:44Z`，大小约 78 MB |
| 桌面快捷方式创建 | 通过 | 是，安装后复核 | `C:\Users\Admin\Desktop\FleshOut Compatible.lnk` 存在 |
| Setup 安装并自动启动 | 通过 | 是 | Setup 启动后安装目录更新到 `2026-06-18 15:32:14 +08:00`，并打开 `FleshOut-Compatible.exe`，窗口标题 `FleshOut Compatible` |
| 未打开外部浏览器 | 通过 | 是，进程复核 | 未发现 Chrome / Edge 浏览器窗口标题包含 FleshOut、localhost 或 127.0.0.1 |
| UI 运行在 WebView2 桌面窗体内 | 通过 | 是，进程复核 | `msedgewebview2.exe` 子进程的 `--webview-exe-name=FleshOut-Compatible.exe` |
| 不显示 localhost 地址栏 | 通过 | 是，窗口与进程复核 | 主窗口标题为 `FleshOut Compatible`，WebView2 窗体无浏览器地址栏 |
| UI 不暴露 localhost | 通过 | 是，进程复核 | 服务监听随机 `127.0.0.1:49448`，仅作为 WebView2 内部服务 |
| 进程层使用随机 `127.0.0.1` 本地端口作为 WebView2 内部服务 | 通过 | 是，安装后复核 | Node 子进程监听 `127.0.0.1:49448`，`GET /api/health` 返回 `ok=true` |
| 命令行窗口未显示，Node 子进程 `CreateNoWindow=true` | 通过 | 是，源码和进程复核 | launcher 设置 `CreateNoWindow=true`；未出现可见 Node 窗口，`conhost.exe` 为隐藏子进程 |
| 原 `fleshout.db` 修改时间未变：`2026-06-18T06:02:58.8740945Z` | 通过 | 是，文件系统复核 | 本次重新打包安装前后读取文件属性一致 |
| 原 FleshOut 项目详情读取与 sidecar 状态同步 | 通过 | 是 | 临时端口 5197 验证原项目 `9503b307-e74a-4331-bea1-7003df22cd16` 返回 25 章与 sidecar 阶段统计 |
| 原 FleshOut 项目阶段 1 执行 | 通过 | 是 | 临时端口 5197 执行 `POST /api/projects/:id/stages/1/run`，25 章完成、0 失败，原库时间戳不变 |
| 新上传项目五阶段门禁 API | 通过 | 是 | 临时端口 5197 创建临时 TXT 项目：初始 stage1=pending，直接 stage2 返回 `WORKFLOW_STAGE_LOCKED`；stage1 完成后 stage3/stage4 仍锁定，stage2 进入 `AI_CALL_CONFIRMATION_REQUIRED`；测试项目已删除 |
| RewritePage 1366x768 / 1400x900 / 1920x1080 视口检查 | 通过 | 是 | 见 `output/playwright/rewrite-responsive-metrics.json` 和三张截图 |
| 原应用式 UI 视觉检查 1366x768 / 1400x900 / 1920x1080 | 通过 | 是 | 见 `output/playwright/ui-redesign-final3-metrics.json`、`ui-redesign-ai-rewrite-final-*.png`、`ui-redesign-projects-final3-1400x900.png` |
| 新建项目导入向导左侧颜色一致性 | 通过 | 是 | `src/styles.css` 中导入向导 active/done 步骤改为暗色作用域样式，构建通过；原库时间戳保持 `2026-06-16T09:54:00.7274080Z` |
| 新建项目上传第一步与 TXT 拆分入口 | 通过 | 是 | Playwright 1400x900 上传 `output/playwright/import-wizard-sample.txt`：文件名和项目名显示，TXT 正文不显示，`下一步` 可点击；点击后进入 `TXT 拆分`，底部主按钮为 `预览` |
| TXT 拆分预览与弹窗滚动行为 | 通过 | 是 | Playwright 1400x900 上传 `output/playwright/import-wizard-lines-sample.txt`：点击 `预览` 后仍在 `TXT 拆分`，footer 为 `下一步`，章节表显示 `L1/L5/L9` 且不显示字数列；弹窗打开时 `bodyOverflow=hidden`，wheel 前后 `window.scrollY` 不变 |
| 预览信息总字数格式 | 通过 | 是 | `src/main.tsx` 中 `formatWanWords()` 以 1 位小数格式化万字；`npm run build` 通过 |
| 模型配置页处理参数 UI | 通过 | 是 | Playwright + Chrome 1400x900 走到第 4 步：只显示 `自动模式`，不显示 `半自动模式` / `手动模式`；并发默认 3、范围 1-30；加料字数默认 4000、范围 1000-20000；截图 `output/playwright/import-wizard-model-config.png` |
| 模型配置页处理参数入库 | 通过 | 是 | 临时端口 5198 创建临时 TXT 项目验证 `default_mode=auto`、`concurrency=7`、`expand_word_count=6000`，随后删除临时项目 |
| 提示词策略页标签切换与编辑 | 通过 | 是 | Playwright + Chrome 1450x860 验证：`场景识别` 显示 14 条并首条展开，`改写规则` 显示 14 条且通用指导编辑器可用，`系统破甲` textarea 可编辑；底部 `下一步` 可见，footer 位于弹窗内；截图与指标见 `output/playwright/import-wizard-prompt-*.png/json` |
| 提示词策略编辑入库 | 通过 | 是 | 临时端口 5199 API 创建临时 TXT 项目，覆盖三段模板后项目 `template_id` 指向新 sidecar 副本，副本三段内容校验通过；验证后临时项目和模板副本已删除 |
| 工作台项目配置弹窗 | 通过 | 是 | Playwright + Chrome 临时端口 5219 创建临时 TXT 项目并打开 `编辑`：基础设置、AI 模型、提示词策略三页签可见；模型列表 3 个、模板 6 个；场景识别/改写规则均解析 14 条；保存后 `concurrency=4`、`expand_word_count=4500`；未触发真实 AI 调用，临时项目已删除 |
| 书籍拆分页 UI | 通过 | 是 | Playwright + Chrome 临时 TXT 项目验证：中间 `.split-stage-empty` 只含书名，右侧只有 `书籍拆分` 进度和 `开始` 按钮，改写动作不可见，左侧导航宽度 64px 且文字隐藏；截图和指标见 `output/playwright/rewrite-split-stage-current.png/json`，临时项目已删除 |
| `GET/PUT /api/settings` | 通过 | 是 | 临时端口 5189 验证：默认来源 `default`，保存后来源 `config`，无效路径返回 400；安装版端口 53767 和快捷方式启动端口 56184 验证 `GET /api/settings` 正常 |
| 旧源码路径自建项目删除 | 通过 | 是 | 安装版 sidecar DB 中项目 `fc2aa4ee-53fa-45e6-a88a-51a64b2b293c` 删除成功，目录和记录移除，原库时间戳不变 |
| 文档经验沉淀 | 通过 | 是 | 新增 `docs/DEVELOPMENT_NOTES.md`，并在 `AGENTS.md`、`ARCHITECTURE_MAP.md`、`QUALITY_GATES.md`、`TROUBLESHOOTING.md`、`CURRENT_FACTS.md` 中建立入口或同步规则 |

## 当前 UI 优化进展

- `WorkbenchPage` 当前是“我的项目/工作台”入口：左侧项目列表，右侧小说详情和 `进入工作台`。
- 项目入口已移除分段筛选，只保留搜索框；项目仍按更新时间排序。
- 项目入口右侧书籍详情页已隐藏 `运行目录`，避免在创建完成后的详情页展示内部文件夹统计。
- `RewritePage` 当前贴近原 FleshOut 暗色桌面工具：64px 左图标栏、顶部五阶段进度线、左侧章节导航、中间阶段内容、右侧阶段操作。
- 五阶段顶部步骤卡现在按前置完成情况逐步解锁，未解锁步骤显示锁定态且不可点击；右侧操作区和阶段底部按钮同步遵守该门禁。
- `书籍拆分` 页中间只保留书籍名称，隐藏书籍基本信息、文件信息和 TXT 正文；右侧只保留拆分进度条和开始/继续按钮，不展示改写本章、查看原文、查看对比等后续阶段动作。
- `内容总结` 未完成时只显示当前步骤启动状态；完成后留在内容总结页，中央展示选中章节的情节概要、登场人物、关键事件，左侧章节导航显示 `需改写` / `无需改写` / `已总结` 标识。
- `内容总结` 对已识别为需改写的章节，在详情下方展示 `场景识别标记`，同一场景会合并显示提示词模板中的识别条件和改写规则。
- 新建项目导入向导左侧步骤栏的当前步骤已改为暗色选中态，不再出现与左上黑灰区域不一致的浅色块。
- 新建项目导入向导第 1 步上传 TXT 后只显示文件名卡片和项目名称，不再展示 TXT 正文；底部 `下一步` 进入第 2 步 `TXT 拆分`。
- `TXT 拆分` 页点击 `预览` 后不再自动跳到“预览信息”，而是在当前页展示完整章节列表；章节标题右侧显示命中行号，便于调试拆章规则。
- `预览信息` 页总字数现在按原应用显示为 `万字`，例如 `251,558` 显示为 `25.2 万字`。
- `模型配置` 页现在只显示自动模式，并提供并发处理数与加料字数设置；创建项目会保存这些值，阶段 2/3/4 会按项目并发数执行。
- `提示词策略` 页现在可在 `系统破甲`、`场景识别`、`改写规则` 三页之间切换；场景规则可展开/收起并编辑字段，改写规则可编辑通用指导和场景特定规则；改动会作为项目专属 sidecar 模板副本保存。
- 工作台项目详情的 `编辑` 按钮现在打开 `项目配置` 弹窗：基础设置页展示项目名称、处理模式、并发处理数和加料字数；AI 模型页可从已接入模型列表中切换；提示词策略页可切换查看系统破甲、场景识别和改写规则。
- 新建项目弹窗打开时会锁定背景页面滚动，滚轮只影响弹窗内可滚动区域。
- 1366x768、1400x900、1920x1080 均保持三栏工作台且无横向滚动。
- 每个阶段通过底部 `StageActionBar` 显示进度和 `开始/继续`；AI改写阶段保留 `重新改写`、`查看原文`、`查看对比`、标记保留和批量改写入口。
- 1180px 及以下切为单列。
- 已用 Playwright + Chrome channel 复核 1366x768、1400x900、1920x1080。
- 本轮开发、调试和打包经验已整理到 `docs/DEVELOPMENT_NOTES.md`，后续改 bug 或前端页面建议先读该文件。

## 当前待验证项

- `test-results/.last-run.json` 存在一次历史 `failed` 状态，但没有失败用例细节；本次改用 Playwright CLI 产出可解释指标和截图。

## 当前遗留问题

- 旧 `AGENT_MEMORY.md` / `README_FIRST_VERSION.md` 中“Web 原型 / Tauri 后续打包”的描述已过时。
- 旧 `AGENT_MEMORY.md` 中“单章 AI 改写未实现”的描述已过时。
- `src/main.tsx` 和 `src/styles.css` 文件较大，后续维护成本高。
- `scripts/build-iexpress.ps1` 文件名仍带 `iexpress`，但当前实际是自定义 C# Setup 生成流程。
- 原 FleshOut 项目可以执行本应用工作流，但删除原项目仍未开放，避免误删原应用数据或源书文件。

## 下一步建议

1. 对设置页做一次 WebView2 内可视验收，确认路径输入框和信息卡在 `1400x900` 下不挤压。
2. 分阶段拆分 `src/main.tsx` 与 `src/styles.css`，先拆页面和共享组件，不在同一任务中混合业务功能改造。
3. 后续如修改安装器脚本，可考虑把 `scripts/build-iexpress.ps1` 改名为 `build-setup.ps1` 并同步 `package.json`。
